<Application x:Class="Repair.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Repair"
             StartupUri="MainWindow.xaml" Startup="App_OnStartup">
    <Application.Resources>
        <DropShadowEffect x:Key="BoxShadow" BlurRadius="10" Direction="-90" RenderingBias="Quality" ShadowDepth="1" />
        <SolidColorBrush x:Key="BackgroundColor" Color="#FFEBEBEB" />
        <SolidColorBrush x:Key="TextColor" Color="#FF404040" />

        <Style x:Key="BoxPanel" TargetType="Border">
            <Setter Property="Effect" Value="{StaticResource BoxShadow}" />
            <Setter Property="Background" Value="{DynamicResource BackgroundColor}" />
            <Setter Property="Margin" Value="7" />
            <Setter Property="CornerRadius" Value="2" />
        </Style>
    </Application.Resources>
</Application>
